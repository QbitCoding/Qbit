package k.utils.common;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

public interface QbitKI extends KI {
	public static final Log LOG=LogFactory.getLog("Qbit");
}
